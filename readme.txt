1. First download the libraries in your pc such as :
  * mysql-connector-python -> pip install mysql-connector-python
2. Then You should download the Mysql in your pc and follow the steps below : 
  * CREATE DATABASE bill;
  * USE bill;
  * CREATE TABLE cart_items(customer_name varchar(35), product_name varchar(30), quantity int, price_per_unit int);
3. In the 73rd line put your mysql passwdf instead of tiger.
